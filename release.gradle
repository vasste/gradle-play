task releaseSql(type: Zip) {
    baseName = rootProject.name
    appendix = 's'
    from('s/')
    include '*.sql'
    destinationDir = buildDir
}

task release(dependsOn: releaseSql, type: Copy) {
    from configurations.releaseDelivery
    into buildDir
}